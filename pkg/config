[vmesh]
initial_listen_port = 10240	; firewalled so only cluster nodes can get to it
processes = 2			; based on number of processors
connections = 3
peers = 100
peer_timeout = 30

[kernel]
max_population = 0		; 0=unlimited (subject to memory)
max_memory = 1024		; in Mb
max_cpu_per_org = 10	; in seconds

