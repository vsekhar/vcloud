#!/bin/bash

if [ "$1" == "-h" ] || [ "$1" == "--help" ]; then
	echo "usage: $0 [pkg]"
	echo
	echo "pkg defaults to 'pkg'"
	exit 2
fi

if [ -z $1 ]; then
	pkg_dir=pkg
else
	pkg_dir=$1
fi

tar cjhf $pkg_dir.tar.bz2 $pkg_dir

